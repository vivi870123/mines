#!/bin/sh

# Sets the background. If given an argument, will set file as background.
[ ! -z "$1" ] && cp "$1" ~/.config/appearance/wallpapers/wall.jpg
xwallpaper --zoom ~/.config/appearance/wallpapers/wall.jpg
